# No API key is required for Beenbe News V11.
# Market data is fetched server-side from Binance's public market-data API.
# The analysis visual is generated locally by the application using SVG + Sharp.
